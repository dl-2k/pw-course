a. Tạo 3 file: file1, file2, file3 
Đáp án: Chưa nằm ở vùng nào hết vì chưa khỏi tạo git init

b. Chạy lệnh: git init
Đáp án: Khởi tạo 3 vùng Working Directory ; Staging Area ; Reposity 

c. Chạy lệnh: git commit -m"init project"
Đáp án: không có gì để commit lên vì chưa chạy git add

d. Chạy lệnh: git add file1
Đáp án: đẩy file1 từ Vùng Working Directory lên Vùng Staging Area

e: Chạy lệnh: git commit -m"add file1"
Đáp án: đẩy file1 lên từ Vùng Staging Area lên Repository

f: Chạy lệnh gi commit -m"add file"
Đáp án: đẩy file từ Vùng Working Directory lên Vùng Staging Area